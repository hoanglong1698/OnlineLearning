import React from 'react'
import { StyleSheet, Text, View } from 'react-native'
import SearchBar from './SearchBarView/search-bar-view'

const Search = () => {
    return (
        <View>
            <SearchBar/>
        </View>
    )
}

export default Search

const styles = StyleSheet.create({})
